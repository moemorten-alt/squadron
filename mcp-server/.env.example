# Copy to .env and fill in values (or set as environment variables)

# URL of the Squadron Java (Spring Boot) backend
SQUADRON_API_URL=http://localhost:8080

# Credentials used by the MCP server to authenticate with the API
SQUADRON_EMAIL=admin@squadron.local
SQUADRON_PASSWORD=admin123
